node {
    stage('Clone repository') {
        checkout scm
    }

    stage('Build image') {
        sh 'echo "build step ok"'
    }

    stage('Push image') {
        sh 'echo "push step ok"'
    }
}